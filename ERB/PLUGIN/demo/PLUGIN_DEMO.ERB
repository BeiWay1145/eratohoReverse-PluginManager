;══════════════════════════════════════════════════════════
;示例插件（demo）
;  演示插件系统完整链路：登记 → 独立开关 → 钩子分发
;  覆盖全部现有钩子点：
;    BOOT        游戏启动（SYSTEM.ERB @EVENTFIRST 内 LOADGLOBAL 之后）
;    SHOP        商店菜单（SHOP.ERB DRAWLINE 之后）
;    EVENTCOMEND 调教结束（EVENTCOMEND.ERB 开头）
;
;  函数命名约定：@PLUGIN_<插件ID>_<事件名>
;  开关位置：配置菜单 [10] 插件管理（GLOBAL:100+槽位，1=开 0=关）
;══════════════════════════════════════════════════════════

;──────────────────────────────
;游戏启动时执行
;──────────────────────────────
@PLUGIN_demo_BOOT
PRINTL (示例插件) 游戏启动完成（BOOT 钩子生效）
RETURN 0

;──────────────────────────────
;进入商店菜单时执行
;──────────────────────────────
@PLUGIN_demo_SHOP
PRINTL (示例插件) 商店菜单已打开（SHOP 钩子生效）
RETURN 0

;──────────────────────────────
;调教结束时执行
;──────────────────────────────
@PLUGIN_demo_EVENTCOMEND
PRINTL (示例插件) 一次调教结束（EVENTCOMEND 钩子生效）
RETURN 0
